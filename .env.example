# Parking Keyboard - Environment Variables
# Copy this file to .env and adjust values

MODE=exit

# Database (use "db" when running with docker-compose)
DB_HOST=localhost
DB_NAME=parking_db
DB_USER=ocr_user
DB_PASS=123456
DB_PORT=3306
